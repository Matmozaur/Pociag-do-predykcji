---
name: python-airflow
description: Airflow DAG or pociag_processing plugin work under airflow. Use when editing Python processing code or tests.
---

# Python Airflow

Read `.github/instructions/python-airflow.instructions.md` before editing. Processing runs as an Airflow plugin, not a standalone service. Use `uv` and validate with the relevant `uv run pytest`, `ruff`, and `mypy` commands from `AGENTS.md` and CI.
