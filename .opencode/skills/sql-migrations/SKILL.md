---
name: sql-migrations
description: SQL schema or migration work in db/migrations. Use when adding or changing database migrations.
---

# SQL Migrations

Read `.github/instructions/sql-migrations.instructions.md` before editing. Create reversible, paired, zero-padded migrations; align schema changes with specs; and use the Makefile migration targets for validation.
